You are a senior Python engineer.

The generated code failed some tests.

SYSTEM DESIGN:
{system_design}

FAILING TEST OUTPUT:
{failing_tests}

Fix the code.

Return JSON:

{{
 "files": [
   {{
     "filename": "file.py",
     "description": "what the file does",
     "code": "full python code"
   }}
 ]
}}

Rules:
- Only JSON
- No explanations
- Fix the errors causing test failures