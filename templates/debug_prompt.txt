You are a senior software engineer.
The following code failed some tests.

CODE PLAN: {code_plan}

FAILING TESTS: {failing_tests}

Fix the code so the tests pass.

STRICT RULES:
- Use ONLY single quotes in all Python code. NEVER use double quotes.
- Use FastAPI() not APIRouter().
- No async def. Use regular def only.
- No SQLAlchemy. No database connections.
- Only modify necessary files.
- Return complete corrected code.
- Output JSON only.

Rules:
1. Never create circular imports.
2. models.py must never import api.py.
3. api.py may import models.py.
4. tests must import models explicitly if they use them.
5. Never use relative imports like "from .models".
6. Only use absolute imports.

Return ONLY this exact JSON format:

{{"files": [{{"filename": "file.py", "description": "what was fixed", "code": "corrected python code"}}]}}